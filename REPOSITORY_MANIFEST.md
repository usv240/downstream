# Downstream repository manifest

This folder is the independent root for the Collaborative Partner submission.

It owns:

- its domain code, fixtures, recordings, tests, web surface, and safety proof;
- a reviewed copy of the shared spine;
- a standalone README, plan, technical design, architecture, research traceability, and submission
  kit;
- its own Git history, public GitHub repository, Cloud Run service, hosted URL, category, and video.

The repository owns its public routes, deployment, domain workflow, state, evidence, interface,
category, and output. Its reviewed runtime substrate is included in full.
