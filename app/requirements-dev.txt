# Everything the README's Verify section needs, and nothing the service needs to run.
#
# These lived only in pyproject's optional extras, so a judge following the README exactly --
# create a venv, install requirements.txt, run pytest -- got "No module named pytest". The
# verification instructions have to work from a clean clone or they are not instructions.
-r requirements.txt

pytest>=8.3,<9.0
httpx>=0.27,<1.0
# Pinned: the rule set moves between releases, and the README quotes a result.
ruff==0.16.4
